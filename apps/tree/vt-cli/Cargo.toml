[package]
name = "vt-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
vt-core = { path = "../vt-core" }
vt-index = { path = "../vt-index" }
vt-snapshot = { path = "../vt-snapshot" }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
