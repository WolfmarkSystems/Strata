# Registry Persistence Fixtures

This corpus provides 30 labeled samples (Windows 10/11) for regression of:

- `forensic_cli registry-persistence`
- timeline `--source registry-persistence`
- execution-correlation enrichment (`registry_persistence_count`)
