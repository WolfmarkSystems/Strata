# SRUM Parser Fixtures

This folder contains lightweight SRUM export fixtures used for parser and CLI regression.

- `manifest.json` lists all fixture files and expected record counts.
- `win10/` contains JSON exports.
- `win11/` contains CSV exports.

These are export-style fixtures for current parser scope; they are not raw `SRUDB.dat` ESE files.
