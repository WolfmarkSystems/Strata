[package]
name = "{{ name }}"
version = "{{ version }}"
authors = ["{{ author }}"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../crates/plugin-sdk" }
serde = { version = "1.0", features = ["derive"] }
