[package]
name = "strata-plugin-conduit"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
strata-plugin-sdk = { path = "../../crates/strata-plugin-sdk" }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

