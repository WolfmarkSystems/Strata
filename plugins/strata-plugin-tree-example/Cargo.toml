[package]
name = "strata-plugin-tree-example"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
strata-tree-sdk = { path = "../../crates/strata-tree-sdk" }
